function area = BurnDown(area, f)

if (rand<f)
    
    i = randi(size(arean,1));
    j = randi(size(area,2));
    
    burningTrees = [i, j];
    
    while(numel(burningTrees)>0)
    
    if(area(burningTrees) == 1))
        
        
    end
    end
end
        